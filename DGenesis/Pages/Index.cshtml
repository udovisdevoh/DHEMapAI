@page
@model DGenesis.Pages.IndexModel
@{
    ViewData["Title"] = "DHEMap Toolkit";
}

<style>
    .hero {
        text-align: center;
        padding: 4rem 1rem;
        margin-bottom: 2rem;
        background-color: #e9ecef;
        border-radius: .3rem;
    }

        .hero h1 {
            font-size: 3.5rem;
            font-weight: 300;
            line-height: 1.2;
        }

        .hero p {
            font-size: 1.25rem;
            font-weight: 300;
        }

    .feature-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 1.5rem;
    }

    .feature-card {
        border: 1px solid #dee2e6;
        border-radius: .25rem;
        padding: 1.5rem;
        display: flex;
        flex-direction: column;
        transition: box-shadow .15s ease-in-out, transform .15s ease-in-out;
    }

        .feature-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 .5rem 1rem rgba(0,0,0,.15);
        }

        .feature-card h2 {
            font-size: 1.5rem;
            margin-top: 0;
            margin-bottom: .75rem;
        }

        .feature-card p {
            flex-grow: 1;
            color: #6c757d;
        }

        .feature-card .btn {
            display: block;
            width: 100%;
            padding: .75rem;
            text-align: center;
            background-color: #007bff;
            color: white;
            text-decoration: none;
            border-radius: .25rem;
            font-weight: 600;
        }

            .feature-card .btn:hover {
                background-color: #0069d9;
            }
</style>

<div class="hero">
    <h1 class="display-4">DHEMap Toolkit</h1>
    <p class="lead">Une suite d'outils pour la génération de cartes assistée par IA.</p>
</div>

<div class="feature-grid">
    <div class="feature-card">
        <h2>Générateur de Prompt (D-Genesis)</h2>
        <p>Créez une directive de conception (prompt) pour l'IA afin qu'elle génère un fichier de thème <code>D-Genesis</code>.</p>
        <a href="/DGenesisCreationPromptGenerator" class="btn">Lancer l'outil</a>
    </div>
    <div class="feature-card">
        <h2>Générateur Aléatoire D-Genesis</h2>
        <p>Créez un fichier D-Genesis aléatoire.</p>
        <a href="/DGenesisRandomGenerator" class="btn">Lancer l'outil</a>
    </div>
    <div class="feature-card">
        <h2>Générateur de Graphe (D-Graph)</h2>
        <p>Utilitaire (HTML) pour générer un prompt qui instruit une IA pour créer un fichier <code>D-Graph</code> complet.</p>
        <a href="/DGraphGenerator " class="btn">Lancer l'outil</a>
    </div>
    <div class="feature-card">
        <h2>Générateur de Forme (DShape)</h2>
        <p>Générez un polygone 2D aléatoire (convexe, concave, symétrique...) pour servir de base à un secteur.</p>
        <a href="/DShapeGenerator" class="btn">Lancer l'outil</a>
    </div>
    <div class="feature-card">
        <h2>Visualiseur de Carte (DHEMap)</h2>
        <p>Un visualiseur (HTML/SVG) pour inspecter la géométrie et le contenu d'un fichier <code>.dhemap.json</code>.</p>
        <a href="/mapviewer.html" class="btn">Ouvrir</a>
    </div>
    <div class="feature-card">
        <h2>Convertisseur DHEMap vers WAD</h2>
        <p>Un outil client (HTML) pour convertir un fichier DHEMap en un fichier <code>.WAD</code> non-compilé.</p>
        <a href="/DHEMapToNonCompiledWad.html" class="btn">Ouvrir</a>
    </div>
    <div class="feature-card">
        <h2>Générateur de Prompt DHEMap</h2>
        <p>Ancien utilitaire (HTML) pour générer des prompts pour la création directe de fichiers <code>DHEMap</code>.</p>
        <a href="/dhemap-generator.html" class="btn">Ouvrir</a>
    </div>
</div>